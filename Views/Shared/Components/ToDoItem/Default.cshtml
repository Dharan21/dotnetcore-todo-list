@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model ToDoList.Models.ToDoItem
@{
    string className = "";
    if (Model.IsCompleted)
    {
        className = "completed";
    }
}

<li class="@className">
    @Model.Task
    <div>
        @if (Model.IsCompleted)
        {
            @Html.ActionLink("Undo Complete", "UndoCompleteTask", "Home", new { @id = Model.Id }, new { @class = "btn btn-primary" })
        }
        else
        {
            @Html.ActionLink("Complete Task", "CompleteTask", "Home", new { @id = Model.Id }, new { @class = "btn btn-primary" })
        }
        @Html.ActionLink("Delete Task", "DeleteTask", "Home", new { @id = Model.Id }, new { @class = "btn btn-danger" })
    </div>
</li>
